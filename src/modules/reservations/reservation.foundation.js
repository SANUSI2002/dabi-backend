// HTTP routes and stock mutation are intentionally deferred to later reservation parts.
export const RESERVATION_HOLD_MINUTES = 20;
export const RESERVATION_STATUS = Object.freeze({ ACTIVE: 'ACTIVE', RELEASED: 'RELEASED', EXPIRED: 'EXPIRED' });
