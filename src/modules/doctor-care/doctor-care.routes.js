import express from 'express';import{protect}from'../../middleware/authMiddleware.js';import{validate}from'../../middleware/validateMiddleware.js';import*as V from'./doctor-care.validator.js';import*as C from'./doctor-care.controller.js';const r=express.Router();r.use(protect);r.get('/doctors',validate(V.listDoctors),C.doctors);r.get('/doctors/:id',validate(V.doctorId),C.doctor);r.post('/relationships',validate(V.request),C.request);r.get('/relationships/patient',C.patientList);r.get('/relationships/doctor',C.doctorList);r.post('/relationships/:id/accept',validate(V.relationId),C.accept);r.post('/relationships/:id/decline',validate(V.relationId),C.decline);r.delete('/relationships/:id',validate(V.relationId),C.revoke);r.use((e,req,res,next)=>res.status(500).json({status:'error',message:'Doctor care module temporarily unavailable'}));export default r;
