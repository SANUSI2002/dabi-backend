import prisma from'../../config/db.js';export const tx=f=>prisma.$transaction(f);export const staff=(t,u)=>t.pharmacyStaffMember.findFirst({where:{pharmacistUserId:u,status:'ACTIVE',pharmacy:{complianceStatus:'VERIFIED'},pharmacist:{professionalProfile:{professionType:'PHARMACIST',verificationStatus:'VERIFIED'}}},select:{pharmacyId:true}});export const queue=(u)=>prisma.orderFulfilment.findMany({where:{pharmacy:{staffMembers:{some:{pharmacistUserId:u,status:'ACTIVE'}}}},include:{order:{select:{reference:true,patientId:true}},allocations:true},orderBy:{createdAt:'asc'}});export const detail=(u,id)=>prisma.orderFulfilment.findFirst({where:{id,pharmacy:{staffMembers:{some:{pharmacistUserId:u,status:'ACTIVE'}}}},include:{order:true,allocations:true}});export const owned=async(t,u,id)=>t.orderFulfilment.findFirst({where:{id,status:'AWAITING_PHARMACIST_REVIEW',pharmacyId:(await staff(t,u))?.pharmacyId},include:{order:true,allocations:true}});export const update=(t,id,data)=>t.orderFulfilment.update({where:{id},data});export const preparation=async(t,u,id,status)=>t.orderFulfilment.findFirst({where:{id,status,pharmacyId:(await staff(t,u))?.pharmacyId},select:{id:true}});export const transition=(t,id,from,to,finalize)=>t.orderFulfilment.updateMany({where:{id,status:from,...(finalize?{inventoryFinalizedAt:null}:{})},data:{status:to,...(finalize?{inventoryFinalizedAt:new Date()}:{})}});export const inventory=(t,id,q)=>t.pharmacyInventoryItem.update({where:{id},data:{availableQuantity:{increment:q}}});export const refund=(t,data)=>t.refundReviewCase.create({data});export const audit=(t,u,type,id)=>t.activityLog.create({data:{userId:u,type,description:'Fulfilment review changed',meta:{id}}});
