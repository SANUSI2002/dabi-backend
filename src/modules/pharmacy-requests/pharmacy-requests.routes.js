import express from 'express';import{protect}from'../../middleware/authMiddleware.js';import{validate}from'../../middleware/validateMiddleware.js';import*as v from'./pharmacy-requests.validator.js';import*as c from'./pharmacy-requests.controller.js';const r=express.Router();r.use(protect);r.post('/staff/invite',validate(v.invite),c.invite);r.post('/staff/:id/accept',validate(v.memberId),c.accept);r.post('/',validate(v.createRequests),c.create);r.get('/patient',validate(v.list),c.patient);r.get('/pharmacy',validate(v.list),c.pharmacy);r.post('/:id/quotes',validate(v.quote),c.quote);r.get('/quotes/patient',validate(v.list),c.quotes);r.use((e,q,s,next)=>s.status(500).json({status:'error',message:'Pharmacy requests module temporarily unavailable'}));export default r;
